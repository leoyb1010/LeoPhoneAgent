# T0 瘦身基线（2026-08-26）

基线提交：`cc033829`  
范围：只标记，不删不确定引用。本文件零行为变更。

## 1. 保留

| 能力 | 主实现 | 路径 |
|---|---|---|
| Provider 配置 | Android Room+JSON 镜像；iOS ProviderConfigStore + B6 拒写空 | `src/android/.../ProviderRepository.kt` `loadConfigSuspending`；`src/ios/Providers/ProviderConfigStore.swift` |
| Agent Loop | 每端一条主循环 | Android `ChatViewModel.runAgentLoop`；iOS `AIChatViewModel.runAgentLoop`；Mac `pi-kernel`；Harmony `localChat.ts` |
| 审批三档 | 现有允许一次 / 本会话 / 拒绝 | iOS `SensitiveToolGate`；Android Offload + Relay 卡；Mac `useSessionApprovals` |
| 中继主路径 | leocodebox relay-client | `src/mac/leocodebox/server/modules/leophone/relay-client.service.ts` |
| Android CLI 方言 | `CliChatEngine` | `src/android/.../sandbox/CliChatEngine.kt` |
| 发版弹窗闸门 | 四端各一份事实源 | Android `whats_new_*`；iOS `LeoReleaseCatalog`；Mac `LEO_RELEASE_NOTES`；Harmony `ReleaseCatalog.ets` |
| Thinking 档位 | iOS 可编辑规则 + Android Catalog + 远程 thinking 字段 | `ThinkingRuleStore.swift`；`ThinkingLevelCatalog.kt`；`MinisHarnessRouter.kt` |
| OpenAI 流式 TTFB | 已是 120s | `OpenAIProvider.STREAM_TTFB_TIMEOUT_MS` |
| Provider 读失败不清空 | Android 保 DB；iOS `[B6] skip save` | 见上 |

## 2. 替换（T1 本船动手）

| 旧行为 | 新主实现 | 删除/收口 |
|---|---|---|
| file_read 只靠 offset/lines，截断后模型猜下一页 | 统一 `next_offset` 契约 | 调用方不再自写分页；三端共用同一算法 |
| Android 设置「连接的设备」硬编码中文 | `stringResource` + 搜索过滤 | 删 SettingsScreen 里三处写死中文 |
| 录音时朗读继续抢麦 | 开录即 hush 朗读 | 不并行第二套 VAD |

## 3. 删除（已确认可删或已删）

| 项 | 状态 |
|---|---|
| iOS 旧 474 行重复更新页 | 已删（PONYTAIL_AUDIT 2026-08-11） |
| 本船：Android 设置硬编码「连接的设备 / 远程机器」 | T1 替换为资源 |

未确认引用、本轮不删：`src/mac/leoagent/`（T4 后 14 天）、Mac `VersionUpgradeModal`、Android `TODO(webapp-hidden)`、iOS 17 以下 Live Activity 死按钮。

## 4. 延期

| 项 | 船 |
|---|---|
| Action Router / 默认截图路径收口 | T2 |
| 设计语言、繁中、无障碍清零 | T2.5 |
| Power 事务引擎 + 热更管道 | T3 |
| 精确窗口 + 删 leoagent | T4 |
| ShellBridge v2 | T5 |
| Relay v2 + 远程会话并入本机列表 | T6 |
| 本地快脑 / 主动卡 | T7 |
| 30 题评测基建 | T2 旁路 |

## 5. 基线数字

| 指标 | 值 | 证据 |
|---|---|---|
| Android version | 1.0.0-alpha.13 / 100013 | `app/build.gradle.kts` |
| iOS | 1.24.1 (95) | `project.pbxproj` |
| Mac | 1.74.2 | `leocodebox/package.json` |
| Harmony | 0.3.0-alpha.14 / 100019 | `AppScope/app.json5` |
| OpenAI TTFB | 120s | `OpenAIProvider.kt` |
| Anthropic/Gemini 读超时 | 10 min | 不降到 30s |
| file_read 硬帽 | 80_000 字符 | Android/iOS 已对齐；Harmony 只截 80k 无分页 |
| 包体 / 冷启动 / 空闲内存 | 待真机 | 本机本轮未装上一公开 APK 实测 |
| 20 题平均步数 | 待 T2 旁路题库 | T1 不承诺步数下降 |

## 6. T1 对账结论（不另起重构）

- B5 首包：OpenAI 已 120s。Anthropic/Gemini 读超时 10 分钟。本船不改这两家超时。
- B6 配置：iOS/Android 均已拒绝读失败后写空。本船不重写 Store。
- B1 推理档：iOS 可编辑规则 + 夹紧已有；Android 身体已收 `thinking`。本船不重做 Catalog。
- B2 读图：iOS 1.24.1 已拦非视觉模型。本船不新开 Vision UI。
