# iPhone 1.36.0 (111) 装机记录

2026-09-12，iPhone 17 Pro Max 已完成覆盖安装。

- 安装前回读：1.35.0 (110)。
- 安装后回读：1.36.0 (111)，Bundle ID `com.leoyuan.leophoneagent`。
- CoreDevice 连接方式回读为 `localNetwork`。
- 唯一装机入口 `scripts/InstallIOSRelease.sh` 返回成功，Release 构建和深度签名校验通过。
- 首次启动被设备锁屏拒绝；后续启动请求成功，返回进程 PID 11815。未使用任何绕过锁屏的方法。
- 后续截图及进程查询遇到 CoreDevice 连接超时，未取得有效截图；启动成功不替代长时间稳定性、完整界面或硬件验收。

## 本版内容与检查

本机 SpeechAnalyzer 与主动语言资源管理；明确 Auto/Offline 优先级和安全的新默认输入分组；录音权限等待身份、停止与中断恢复；语音执行位置/部分结果标识；iOS 远程日志保存状态。已有用户分组保留。

396 项逻辑测试全部通过。发版记录、动效/无障碍、可见控件、原生权限、相机取消、设备控制、录音中断与模式选择检查通过。新资源页此前已通过独立模拟器场景的布局核对，不能冒充本次真机截图。

装机后另修正了构建脚本：Release 的 DebugSkillGenerated.swift 只有内容变化才替换，避免每次改写时间戳导致整模块重新编译。实际构建阶段提取测试先复现问题，再验证内容/mtime 稳定、旧内容更新及临时文件清理。此修改不改变交付 App 的运行时生成内容。

证据目录：`outputs/implementation-2026-09-12/ios-delivery-1.36.0/`。

完整 W01–W23 继续进行；语言资源下载与真实转写、电话/Siri/耳机组合、Mac 语音适配等未据此标为完成。
