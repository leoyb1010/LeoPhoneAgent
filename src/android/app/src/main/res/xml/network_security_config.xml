<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Credentials and prompts must not cross plaintext LAN/provider links.
         Loopback HTTP remains available for local OAuth callbacks only. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- OAuth callback forwarding is device-local and contains no provider
         bearer token after the state/code exchange completes. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
