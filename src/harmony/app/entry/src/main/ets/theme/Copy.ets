export class Copy {
  static readonly readyTitle: string = '本机 Agent 已就绪';
  static readonly readySubtitle: string = '直接在这台手机上开始任务，文件和执行权始终由你控制。';
  static readonly localBadge: string = '本机运行';
  static readonly privateBadge: string = '默认私密';
  static readonly taskPhone: string = '操作这台手机';
  static readonly taskResearch: string = '调研并整理结论';
  static readonly taskAutomate: string = '创建自动化';
  static readonly promptPhone: string = '帮我在手机上完成这件事：';
  static readonly promptResearch: string = '调研这个主题，对比可靠来源并给出简明结论：';
  static readonly promptAutomate: string = '为这件事创建安全、可重复执行的自动化：';
  static readonly welcomeTitle: string = '认识你的本机操作员';
  static readonly welcomeSubtitle: string = '连接一个模型，即可用同一个私密 Agent 读写沙箱文件、记事，并连上你的 Mac。';
  static readonly localRuntime: string = '本机运行时已安装';
  static readonly step1: string = '连接智能模型';
  static readonly step1sub: string = '设置 → 管理供应商，先选类型再填钥匙';
  static readonly step2: string = '选择执行模型';
  static readonly step2sub: string = '在设置里填本机模型';
  static readonly step3: string = '启动第一个任务';
  static readonly step3sub: string = '点击开始';
  static readonly stepDone: string = '已完成';
  static readonly stepLocked: string = '请先完成上一步';
  static readonly searchPlaceholder: string = '搜索会话…';
  static readonly noLinux: string = '鸿蒙 NEXT 跑不了 Android 那份 PRoot ELF。写文件只在应用沙箱，并默认询问。';
  static readonly pushB: string = '正在向 Push Kit 要 token。没配 AGC 时杀进程后仍然收不到审批。';
}
