/// The pure tool-preflight source is compiled directly into MinisTests without
/// the production view model. This empty namespace preserves its extension API
/// while keeping the unit-test target independent from UIKit and the agent UI.
final class AIChatViewModel {}
